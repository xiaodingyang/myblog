# 后端 API 接口测试用例

> 覆盖所有新增和修改的后端 API 接口

---

## 一、通知系统（notifications）

### TASK-12：通知 API

#### BA-001：未登录用户访问通知接口返回 401

| 字段 | 内容 |
|------|------|
| **测试 ID** | BA-001 |
| **对应功能** | TASK-12 评论回复通知 |
| **优先级** | P0 |
| **前置条件** | 不携带 Authorization header |
| **测试步骤** | 1. `GET /api/notifications` 无 token<br>2. 断言返回 HTTP 401 |
| **期望结果** | 未认证请求被拒绝 |

#### BA-002：获取当前用户通知列表（分页）

| 字段 | 内容 |
|------|------|
| **测试 ID** | BA-002 |
| **对应功能** | TASK-12 评论回复通知 |
| **优先级** | P0 |
| **前置条件** | 用户 A 已登录，数据库中有 25 条通知 |
| **测试步骤** | 1. `GET /api/notifications?page=1&pageSize=20` 携带用户 A token<br>2. 断言返回 HTTP 200<br>3. 断言返回结构 `{ code: 0, data: { list: [...], total: 25 } }`<br>4. 断言 list 长度为 20<br>5. 断言每条通知包含 `fromUser`（已 populate 用户名头像）、`articleId`、`content`、`isRead`、`createdAt`<br>6. 断言按 `createdAt` 倒序排列 |
| **期望结果** | 返回分页通知列表，关联数据已填充 |

#### BA-003：获取第二页通知

| 字段 | 内容 |
|------|------|
| **测试 ID** | BA-003 |
| **对应功能** | TASK-12 评论回复通知 |
| **优先级** | P1 |
| **前置条件** | 同 BA-002，25 条通知 |
| **测试步骤** | 1. `GET /api/notifications?page=2&pageSize=20`<br>2. 断言 list 长度为 5<br>3. 断言 total 仍为 25 |
| **期望结果** | 分页正确，第二页返回剩余 5 条 |

#### BA-004：获取未读通知数量

| 字段 | 内容 |
|------|------|
| **测试 ID** | BA-004 |
| **对应功能** | TASK-12 评论回复通知 |
| **优先级** | P0 |
| **前置条件** | 用户 A 有 3 条未读通知 |
| **测试步骤** | 1. `GET /api/notifications/unread-count` 携带 token<br>2. 断言返回 `{ code: 0, data: { count: 3 } }` |
| **期望结果** | 正确返回未读数量 |

#### BA-005：标记单条通知已读

| 字段 | 内容 |
|------|------|
| **测试 ID** | BA-005 |
| **对应功能** | TASK-12 评论回复通知 |
| **优先级** | P0 |
| **前置条件** | 通知 ID `n1` 属于用户 A，`isRead: false` |
| **测试步骤** | 1. `PUT /api/notifications/n1/read` 携带用户 A token<br>2. 断言返回 HTTP 200<br>3. 查询数据库，断言通知 `n1` 的 `isRead === true` |
| **期望结果** | 通知标记为已读 |

#### BA-006：标记不属于自己的通知返回 403

| 字段 | 内容 |
|------|------|
| **测试 ID** | BA-006 |
| **对应功能** | TASK-12 评论回复通知 |
| **优先级** | P1 |
| **前置条件** | 通知 ID `n2` 属于用户 B |
| **测试步骤** | 1. `PUT /api/notifications/n2/read` 携带用户 A token<br>2. 断言返回 HTTP 403 |
| **期望结果** | 不能标记他人的通知 |

#### BA-007：全部标记已读

| 字段 | 内容 |
|------|------|
| **测试 ID** | BA-007 |
| **对应功能** | TASK-12 评论回复通知 |
| **优先级** | P0 |
| **前置条件** | 用户 A 有 5 条未读通知 |
| **测试步骤** | 1. `PUT /api/notifications/read-all` 携带用户 A token<br>2. 断言返回 HTTP 200<br>3. 查询数据库，断言用户 A 所有通知 `isRead === true`<br>4. 调用 `GET /api/notifications/unread-count` → 断言 count === 0 |
| **期望结果** | 所有通知标记已读，未读数清零 |

#### BA-008：评论文章时自动创建通知给其他评论者

| 字段 | 内容 |
|------|------|
| **测试 ID** | BA-008 |
| **对应功能** | TASK-12 评论回复通知（commentController 逻辑） |
| **优先级** | P0 |
| **前置条件** | 用户 A 已在文章 `art1` 下评论；用户 B 已登录 |
| **测试步骤** | 1. 用户 B 对文章 `art1` 发表评论：`POST /api/comments` body: `{ articleId: 'art1', content: '好文章！' }`<br>2. 断言评论创建成功<br>3. 查询 Notification 集合，断言存在一条通知：`toUser` 为用户 A，`fromUser` 为用户 B，`type` 为 `'reply'`，`isRead` 为 false |
| **期望结果** | 评论成功，其他评论者收到通知 |

---

## 二、文章系列（series）

### TASK-16：系列 API

#### BA-009：获取所有系列列表（公开）

| 字段 | 内容 |
|------|------|
| **测试 ID** | BA-009 |
| **对应功能** | TASK-16 文章系列/专栏 |
| **优先级** | P0 |
| **前置条件** | 数据库中有 3 个系列，每个系列关联不同数量的文章 |
| **测试步骤** | 1. `GET /api/series`（无需认证）<br>2. 断言返回 HTTP 200<br>3. 断言返回 3 个系列<br>4. 断言每个系列包含 `articleCount` 字段 |
| **期望结果** | 公开接口返回所有系列及文章数量 |

#### BA-010：获取系列详情

| 字段 | 内容 |
|------|------|
| **测试 ID** | BA-010 |
| **对应功能** | TASK-16 文章系列/专栏 |
| **优先级** | P1 |
| **前置条件** | 系列 `s1` 存在 |
| **测试步骤** | 1. `GET /api/series/s1`<br>2. 断言返回 `{ code: 0, data: { _id: 's1', title: '...', description: '...', cover: '...' } }` |
| **期望结果** | 返回系列完整信息 |

#### BA-011：获取系列下的文章列表

| 字段 | 内容 |
|------|------|
| **测试 ID** | BA-011 |
| **对应功能** | TASK-16 文章系列/专栏 |
| **优先级** | P0 |
| **前置条件** | 系列 `s1` 关联 3 篇文章，seriesOrder 分别为 3、1、2 |
| **测试步骤** | 1. `GET /api/series/s1/articles`<br>2. 断言返回 3 篇文章<br>3. 断言按 `seriesOrder` 升序排列（1 → 2 → 3）<br>4. 断言每篇文章包含 `title`、`cover`、`seriesOrder` 字段 |
| **期望结果** | 返回系列文章，按序号排序 |

#### BA-012：创建系列（管理员）

| 字段 | 内容 |
|------|------|
| **测试 ID** | BA-012 |
| **对应功能** | TASK-16 文章系列/专栏 |
| **优先级** | P0 |
| **前置条件** | 管理员 token，body: `{ title: 'React 入门系列', description: '从零学习 React' }` |
| **测试步骤** | 1. `POST /api/series` 携带管理员 token<br>2. 断言返回 HTTP 201（或 200）<br>3. 断言返回创建的系列数据，包含 `_id`<br>4. 查询数据库确认系列已创建 |
| **期望结果** | 管理员可创建系列 |

#### BA-013：非管理员创建系列返回 403

| 字段 | 内容 |
|------|------|
| **测试 ID** | BA-013 |
| **对应功能** | TASK-16 文章系列/专栏 |
| **优先级** | P1 |
| **前置条件** | 普通用户 token |
| **测试步骤** | 1. `POST /api/series` 携带普通用户 token<br>2. 断言返回 HTTP 403 |
| **期望结果** | 权限不足被拒绝 |

#### BA-014：更新系列（管理员）

| 字段 | 内容 |
|------|------|
| **测试 ID** | BA-014 |
| **对应功能** | TASK-16 文章系列/专栏 |
| **优先级** | P0 |
| **前置条件** | 系列 `s1` 存在，管理员 token |
| **测试步骤** | 1. `PUT /api/series/s1` body: `{ title: 'React 进阶系列', description: '深入学习 React' }`<br>2. 断言返回 HTTP 200<br>3. 查询数据库，断言标题和描述已更新 |
| **期望结果** | 管理员可更新系列信息 |

#### BA-015：删除系列并解除文章关联

| 字段 | 内容 |
|------|------|
| **测试 ID** | BA-015 |
| **对应功能** | TASK-16 文章系列/专栏 |
| **优先级** | P0 |
| **前置条件** | 系列 `s1` 关联 2 篇文章，管理员 token |
| **测试步骤** | 1. `DELETE /api/series/s1` 携带管理员 token<br>2. 断言返回 HTTP 200<br>3. 查询数据库，断言系列 `s1` 已删除<br>4. 查询关联文章，断言 `series` 字段为 `null`（解除关联） |
| **期望结果** | 系列删除后，关联文章的 series 字段被清空 |

#### BA-016：获取不存在的系列返回 404

| 字段 | 内容 |
|------|------|
| **测试 ID** | BA-016 |
| **对应功能** | TASK-16 文章系列/专栏 |
| **优先级** | P2 |
| **前置条件** | 无 |
| **测试步骤** | 1. `GET /api/series/invalid-id`<br>2. 断言返回 HTTP 404 |
| **期望结果** | 不存在的系列返回 404 |

#### BA-017：创建系列缺少必填字段返回 400

| 字段 | 内容 |
|------|------|
| **测试 ID** | BA-017 |
| **对应功能** | TASK-16 文章系列/专栏 |
| **优先级** | P1 |
| **前置条件** | 管理员 token |
| **测试步骤** | 1. `POST /api/series` body: `{ description: '无标题系列' }`（缺少 title）<br>2. 断言返回 HTTP 400 |
| **期望结果** | 缺少必填字段时返回参数错误 |

#### BA-018：系列标题超过 100 字符被截断或报错

| 字段 | 内容 |
|------|------|
| **测试 ID** | BA-018 |
| **对应功能** | TASK-16 文章系列/专栏 |
| **优先级** | P2 |
| **前置条件** | 管理员 token |
| **测试步骤** | 1. `POST /api/series` body: `{ title: 'A'.repeat(101) }`<br>2. 断言返回 HTTP 400 或 title 被 trim 后存储 |
| **期望结果** | 超长标题被正确校验 |

---

## 三、图片上传 WebP（uploadController）

### TASK-17：WebP 图片处理

#### BA-019：上传 JPEG 图片同时生成 WebP 和缩略图

| 字段 | 内容 |
|------|------|
| **测试 ID** | BA-019 |
| **对应功能** | TASK-17 WebP 图片优化 |
| **优先级** | P0 |
| **前置条件** | 准备一张 JPEG 测试图片（800x600） |
| **测试步骤** | 1. `POST /api/upload` form-data: `file=test.jpg`<br>2. 断言返回 HTTP 200<br>3. 断言返回包含 `{ url, webpUrl, thumbUrl, thumbWebpUrl }` 四个字段<br>4. 检查 uploads 目录，断言同时存在 `test.jpg`、`test.webp`、`test_thumb.jpg`、`test_thumb.webp` |
| **期望结果** | 上传成功，自动生成 WebP 和缩略图 |

#### BA-020：WebP 文件体积小于原图

| 字段 | 内容 |
|------|------|
| **测试 ID** | BA-020 |
| **对应功能** | TASK-17 WebP 图片优化 |
| **优先级** | P1 |
| **前置条件** | 同 BA-019 |
| **测试步骤** | 1. 上传后获取原图和 WebP 文件大小<br>2. 断言 WebP 文件体积 < 原图体积 |
| **期望结果** | WebP 压缩有效 |

#### BA-021：未选择文件返回 400

| 字段 | 内容 |
|------|------|
| **测试 ID** | BA-021 |
| **对应功能** | TASK-17 WebP 图片优化 |
| **优先级** | P1 |
| **前置条件** | 无 |
| **测试步骤** | 1. `POST /api/upload` 不带 file 字段<br>2. 断言返回 HTTP 400 |
| **期望结果** | 缺少文件时返回错误 |

#### BA-022：sharp 未安装时跳过 WebP 生成，仅返回原图 URL

| 字段 | 内容 |
|------|------|
| **测试 ID** | BA-022 |
| **对应功能** | TASK-17 WebP 图片优化 |
| **优先级** | P2 |
| **前置条件** | Mock sharp 导入失败（模拟未安装场景） |
| **测试步骤** | 1. `POST /api/upload` 上传图片<br>2. 断言上传成功，返回 `url` 字段<br>3. 断言 `webpUrl` 字段为空或等于 `url` |
| **期望结果** | sharp 不可用时优雅降级，不阻止上传 |

---

## 四、评论控制器（commentController 变更）

### TASK-12：评论创建通知

#### BA-023：评论频率限制（30 秒内不允许重复评论）

| 字段 | 内容 |
|------|------|
| **测试 ID** | BA-023 |
| **对应功能** | TASK-12 评论频率限制 |
| **优先级** | P1 |
| **前置条件** | 用户 A 刚发表过评论 |
| **测试步骤** | 1. 用户 A 发表评论 → 成功<br>2. 间隔 5 秒再次评论<br>3. 断言返回 HTTP 429 或频率限制错误<br>4. 等待 30 秒后再次评论<br>5. 断言成功 |
| **期望结果** | 30 秒内重复评论被拦截，30 秒后恢复 |

#### BA-024：通知创建失败不影响评论发表

| 字段 | 内容 |
|------|------|
| **测试 ID** | BA-024 |
| **对应功能** | TASK-12 评论回复通知 |
| **优先级** | P1 |
| **前置条件** | Mock Notification.create 抛出异常 |
| **测试步骤** | 1. 用户 B 发表评论<br>2. 断言评论创建成功（HTTP 200）<br>3. 断言评论已存入数据库<br>4. 断言通知创建失败不影响评论返回 |
| **期望结果** | 通知异常被捕获，评论正常返回 |

#### BA-025：评论点赞/取消点赞（原子操作）

| 字段 | 内容 |
|------|------|
| **测试 ID** | BA-025 |
| **对应功能** | 评论交互（commentController.likeComment） |
| **优先级** | P1 |
| **前置条件** | 用户 A 已登录，评论 `c1` 存在 |
| **测试步骤** | 1. `PUT /api/comments/c1/like` 携带用户 A token<br>2. 断言点赞成功，`likes` 数组包含用户 A ID<br>3. 再次调用相同接口<br>4. 断言取消点赞，`likes` 数组不再包含用户 A ID |
| **期望结果** | 点赞/取消点赞切换正常，原子操作无并发问题 |

---

## 五、Article 模型变更

### TASK-16：Article 模型新增 series 字段

#### BA-026：文章可设置 series 和 seriesOrder

| 字段 | 内容 |
|------|------|
| **测试 ID** | BA-026 |
| **对应功能** | TASK-16 文章系列/专栏 |
| **优先级** | P1 |
| **前置条件** | 系列 `s1` 存在 |
| **测试步骤** | 1. 创建文章，body 包含 `series: 's1', seriesOrder: 2`<br>2. 查询数据库，断言文章 `series` 字段为 `s1` ObjectId<br>3. 断言 `seriesOrder` 为 2 |
| **期望结果** | 文章正确关联系列和序号 |

---

## 六、模型注册

### TASK-12 + TASK-16：models/index.js 导出

#### BA-027：Notification 和 Series 模型正确注册

| 字段 | 内容 |
|------|------|
| **测试 ID** | BA-027 |
| **对应功能** | TASK-12 + TASK-16 |
| **优先级** | P1 |
| **前置条件** | 无 |
| **测试步骤** | 1. `require('../src/models')`<br>2. 断言导出对象包含 `Notification` 键<br>3. 断言 `Notification` 是 Mongoose Model<br>4. 断言导出对象包含 `Series` 键<br>5. 断言 `Series` 是 Mongoose Model |
| **期望结果** | 新模型正确导出 |

---

## 七、RSS 路由

### RSS Feed

#### BA-028：RSS Feed 返回有效的 XML

| 字段 | 内容 |
|------|------|
| **测试 ID** | BA-028 |
| **对应功能** | RSS 订阅路由 |
| **优先级** | P2 |
| **前置条件** | 数据库中有已发布文章 |
| **测试步骤** | 1. `GET /rss`<br>2. 断言返回 HTTP 200<br>3. 断言 Content-Type 包含 `application/xml`<br>4. 解析 XML，断言包含 `<rss>` 根元素和 `<channel>`<br>5. 断言 `<item>` 数量 ≤ 20<br>6. 断言每个 item 包含 `<title>`、`<link>`、`<description>`、`<pubDate>` |
| **期望结果** | 返回格式正确的 RSS 2.0 Feed |
