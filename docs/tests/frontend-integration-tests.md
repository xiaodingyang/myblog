# 前端集成测试用例

> 覆盖页面级功能整合，验证组件交互、路由跳转、数据流等

---

## 一、首页（home/index.tsx）

### TASK-06 + TASK-10 + TASK-11：首页数据加载与推荐

#### FI-001：首页首次加载显示骨架屏，数据返回后渲染内容

| 字段 | 内容 |
|------|------|
| **测试 ID** | FI-001 |
| **对应功能** | TASK-08 骨架屏 + TASK-10 API 缓存 |
| **优先级** | P0 |
| **前置条件** | Mock `/api/articles`、`/api/categories`、`/api/tags` 接口，延迟 500ms 返回 |
| **测试步骤** | 1. 访问首页 `/`<br>2. 断言初始状态渲染 `<HomeSkeleton />`（骨架屏可见）<br>3. 等待 500ms 数据返回<br>4. 断言骨架屏消失，文章卡片渲染完成<br>5. 断言精选文章区域、最新文章区域、"为你推荐"区域均已渲染 |
| **期望结果** | 骨架屏过渡到实际内容，三个文章区域正确显示 |

#### FI-002：首页"为你推荐"展示未读热门文章

| 字段 | 内容 |
|------|------|
| **测试 ID** | FI-002 |
| **对应功能** | TASK-11 猜你喜欢推荐 |
| **优先级** | P1 |
| **前置条件** | Mock 文章列表 API 返回 6 篇文章（views: [500, 200, 100, 50, 20, 10]）；设置 localStorage `reading_history` 包含 1 篇已读文章 |
| **测试步骤** | 1. 访问首页<br>2. 等待数据加载完成<br>3. 查找"为你推荐"区域<br>4. 断言推荐区域不包含已读文章<br>5. 断言推荐文章按 views 降序排列<br>6. 断言最多展示 3 篇 |
| **期望结果** | 推荐区域排除已读，展示热门文章，最多 3 篇 |

#### FI-003：cachedRequest 二次访问首页命中缓存

| 字段 | 内容 |
|------|------|
| **测试 ID** | FI-003 |
| **对应功能** | TASK-10 接口缓存层 |
| **优先级** | P1 |
| **前置条件** | 首次访问已完成（数据已缓存） |
| **测试步骤** | 1. 首次访问首页，记录 API 请求次数<br>2. 切换到分类页 `/categories`<br>3. 返回首页 `/`<br>4. 断言 Network 面板无新的 `/api/articles`、`/api/categories`、`/api/tags` 请求<br>5. 断言页面内容与首次一致 |
| **期望结果** | 二次访问命中缓存，无重复请求，页面渲染正确 |

---

## 二、文章详情页（articles/detail.tsx）

### TASK-06 + TASK-14 + TASK-13：详情页数据与交互

#### FI-004：从首页预加载缓存直接渲染文章（无 loading 状态）

| 字段 | 内容 |
|------|------|
| **测试 ID** | FI-004 |
| **对应功能** | TASK-06 文章卡片悬停预加载 |
| **优先级** | P1 |
| **前置条件** | 首页悬停某文章卡片，预加载已完成（`prefetch` 缓存已有数据） |
| **测试步骤** | 1. 在首页悬停文章卡片 "React Hooks 详解"<br>2. 等待 `/api/articles/{id}` 请求完成<br>3. 点击卡片进入详情页<br>4. 断言页面直接显示文章内容，未出现 loading 状态<br>5. 断言无额外的文章详情 API 请求 |
| **期望结果** | 预加载数据命中，页面秒渲染 |

#### FI-005：图片灯箱在文章详情页正常工作

| 字段 | 内容 |
|------|------|
| **测试 ID** | FI-005 |
| **对应功能** | TASK-14 图片灯箱查看器 |
| **优先级** | P1 |
| **前置条件** | 文章正文包含 3 张图片 |
| **测试步骤** | 1. 打开文章详情页，等待内容渲染<br>2. 点击第 2 张图片<br>3. 断言灯箱弹出，显示当前图片<br>4. 点击右箭头 → 断言切换到第 3 张图片<br>5. 按 ESC → 断言灯箱关闭 |
| **期望结果** | 文章内图片点击打开灯箱，可导航切换 |

#### FI-006：阅读文章触发成就检查

| 字段 | 内容 |
|------|------|
| **测试 ID** | FI-006 |
| **对应功能** | TASK-13 阅读成就系统 |
| **优先级** | P2 |
| **前置条件** | 清空 localStorage 的 `blog_achievements` 和 `reading_history` |
| **测试步骤** | 1. 打开第一篇文章详情页<br>2. 等待 `addReadingHistory` + `checkAchievements` 执行<br>3. 断言页面顶部显示 toast "成就解锁：初来乍到 — 阅读了第一篇文章"<br>4. 检查 localStorage `blog_achievements` 包含 `first_read` 成就 |
| **期望结果** | 首次阅读触发成就解锁 toast 和持久化 |

---

## 三、文章列表页（articles/index.tsx）

### TASK-10 + TASK-19：列表页缓存与渲染

#### FI-007：文章列表页使用 cachedRequest 并支持筛选

| 字段 | 内容 |
|------|------|
| **测试 ID** | FI-007 |
| **对应功能** | TASK-10 接口缓存层 |
| **优先级** | P1 |
| **前置条件** | Mock `/api/articles` 接口 |
| **测试步骤** | 1. 访问文章列表页 `/articles`<br>2. 等待数据加载，确认 API 被调用<br>3. 选择分类筛选 "React"<br>4. 断言发起新的 API 请求（参数含 category）<br>5. 清除筛选回到全部<br>6. 断言命中缓存，不重复请求 |
| **期望结果** | 缓存对相同查询参数生效，筛选触发新请求 |

#### FI-008：文章列表 React.memo 优化不误渲染

| 字段 | 内容 |
|------|------|
| **测试 ID** | FI-008 |
| **对应功能** | TASK-19 组件渲染优化 |
| **优先级** | P2 |
| **前置条件** | 列表页已加载 12 篇文章 |
| **测试步骤** | 1. 使用 React DevTools Profiler 开始录制<br>2. 修改筛选条件触发列表更新<br>3. 停止录制<br>4. 检查 ArticleCard 渲染记录<br>5. 断言：新列表中的卡片仅因数据变化而渲染，未变化的卡片未重渲染 |
| **期望结果** | 仅数据变化的 ArticleCard 重渲染 |

---

## 四、全局搜索（GlobalSearch + FrontLayout）

### TASK-07：全局搜索集成

#### FI-009：Ctrl+K 唤起搜索浮层并完成搜索流程

| 字段 | 内容 |
|------|------|
| **测试 ID** | FI-009 |
| **对应功能** | TASK-07 全局搜索浮层 |
| **优先级** | P0 |
| **前置条件** | 在首页，Mock `/api/articles?keyword=xxx` 接口 |
| **测试步骤** | 1. 按 `Ctrl+K`（Mac: `Cmd+K`）<br>2. 断言搜索 Modal 弹出，Input 自动聚焦<br>3. 输入 "React"<br>4. 等待 300ms 防抖<br>5. 断言搜索结果列表显示匹配文章<br>6. 断言搜索关键词在标题中高亮（黄色 `<mark>` 标签）<br>7. 按 ESC 关闭搜索 |
| **期望结果** | Ctrl+K → 输入 → 防抖 → 搜索结果 → 关闭，全流程正常 |

#### FI-010：搜索结果点击跳转到文章详情页

| 字段 | 内容 |
|------|------|
| **测试 ID** | FI-010 |
| **对应功能** | TASK-07 全局搜索浮层 |
| **优先级** | P1 |
| **前置条件** | 搜索结果已加载 |
| **测试步骤** | 1. 搜索 "React"，等待结果<br>2. 点击第 1 条搜索结果<br>3. 断言 Modal 关闭<br>4. 断言路由跳转到 `/article/{id}`<br>5. 断言文章详情页正确渲染 |
| **期望结果** | 点击搜索结果跳转到对应文章页 |

---

## 五、分类/标签页缓存

### TASK-10：分类页和标签页缓存

#### FI-011：分类页使用 cachedRequest 加载分类

| 字段 | 内容 |
|------|------|
| **测试 ID** | FI-011 |
| **对应功能** | TASK-10 接口缓存层 |
| **优先级** | P2 |
| **前置条件** | Mock `/api/categories` 接口 |
| **测试步骤** | 1. 首次访问 `/categories`，断言 API 调用<br>2. 导航到其他页面<br>3. 再次访问 `/categories`<br>4. 断言无新 API 请求（缓存命中，TTL 30min） |
| **期望结果** | 分类数据在 TTL 内命中缓存 |

#### FI-012：标签页使用 cachedRequest 加载标签

| 字段 | 内容 |
|------|------|
| **测试 ID** | FI-012 |
| **对应功能** | TASK-10 接口缓存层 |
| **优先级** | P2 |
| **前置条件** | Mock `/api/tags` 接口 |
| **测试步骤** | 1. 首次访问 `/tags`，断言 API 调用<br>2. 导航离开再返回<br>3. 断言缓存命中 |
| **期望结果** | 标签数据在 TTL 内命中缓存 |

---

## 六、通知系统集成

### TASK-12：通知铃铛在布局中的集成

#### FI-013：登录用户看到通知铃铛，未登录不可见

| 字段 | 内容 |
|------|------|
| **测试 ID** | FI-013 |
| **对应功能** | TASK-12 评论回复通知 |
| **优先级** | P1 |
| **前置条件** | 分别准备已登录和未登录状态 |
| **测试步骤** | 1. 未登录状态访问首页 → 断言无 NotificationBell 组件<br>2. 模拟 GitHub 登录 → `isLoggedIn = true`<br>3. 断言导航栏右侧出现通知铃铛图标 |
| **期望结果** | 仅登录用户可见通知铃铛 |

#### FI-014：通知铃铛到文章详情页的完整流程

| 字段 | 内容 |
|------|------|
| **测试 ID** | FI-014 |
| **对应功能** | TASK-12 评论回复通知 |
| **优先级** | P1 |
| **前置条件** | 用户已登录，Mock 通知 API 返回 1 条未读通知 |
| **测试步骤** | 1. 访问首页 → 铃铛显示未读数 "1"<br>2. 点击铃铛 → Popover 显示通知列表<br>3. 点击通知项 → 跳转到对应文章页<br>4. 在文章页发表评论<br>5. 另一用户在同一文章评论<br>6. 刷新页面 → 铃铛未读数增加 |
| **期望结果** | 通知→跳转→评论→新通知，全流程通畅 |

---

## 七、系列导航集成

### TASK-16：系列在文章详情页的展示

#### FI-015：有系列的文章显示系列导航

| 字段 | 内容 |
|------|------|
| **测试 ID** | FI-015 |
| **对应功能** | TASK-16 文章系列/专栏 |
| **优先级** | P1 |
| **前置条件** | 文章 article1 属于 series1，Mock `/api/series/s1/articles` 返回 3 篇文章 |
| **测试步骤** | 1. 打开 article1 详情页<br>2. 断言正文下方出现系列导航卡片<br>3. 断言显示系列标题<br>4. 断言当前文章高亮<br>5. 点击"下一篇" → 跳转到 article2 详情页 |
| **期望结果** | 系列导航正确展示，导航跳转正常 |

#### FI-016：无系列的文章不显示系列导航

| 字段 | 内容 |
|------|------|
| **测试 ID** | FI-016 |
| **对应功能** | TASK-16 文章系列/专栏 |
| **优先级** | P2 |
| **前置条件** | 文章无 series 字段 |
| **测试步骤** | 1. 打开无系列文章详情页<br>2. 断言页面无 `<SeriesNav>` 组件渲染 |
| **期望结果** | 无系列关联时不显示导航 |

---

## 八、相关推荐集成

### TASK-11：RelatedArticles 三级推荐策略

#### FI-017：相关推荐优先展示同标签文章

| 字段 | 内容 |
|------|------|
| **测试 ID** | FI-017 |
| **对应功能** | TASK-11 猜你喜欢推荐 |
| **优先级** | P1 |
| **前置条件** | 当前文章有 tags: ["React", "Hooks"]；Mock API 按标签搜索返回 3 篇文章 |
| **测试步骤** | 1. 打开文章详情页<br>2. 等待推荐加载<br>3. 断言推荐文章列表中前几篇与当前文章有共同标签<br>4. 断言不包含当前文章本身<br>5. 断言不包含已读文章 |
| **期望结果** | 推荐列表优先匹配标签，排除当前和已读文章 |

#### FI-018：同标签文章不足时补充分类和热门文章

| 字段 | 内容 |
|------|------|
| **测试 ID** | FI-018 |
| **对应功能** | TASK-11 猜你喜欢推荐 |
| **优先级** | P2 |
| **前置条件** | 当前文章标签 "ZzzUnique"，Mock 按标签搜索返回 0 篇，按分类返回 1 篇，热门列表返回多篇文章 |
| **测试步骤** | 1. 打开文章详情页<br>2. 等待推荐加载<br>3. 断言推荐列表不为空<br>4. 断言包含同分类文章<br>5. 断言补充了热门文章 |
| **期望结果** | 三级推荐策略正确降级补充 |

---

## 九、移动端集成

### TASK-18：MobileTabBar + useSwipe 集成

#### FI-019：MobileTabBar 在移动端显示、PC 端隐藏

| 字段 | 内容 |
|------|------|
| **测试 ID** | FI-019 |
| **对应功能** | TASK-18 移动端底部导航栏 |
| **优先级** | P1 |
| **前置条件** | 设置视口宽度为 375px（移动端）和 1280px（PC 端） |
| **测试步骤** | 1. 375px 宽度访问首页 → 断言底部显示 MobileTabBar<br>2. 1280px 宽度访问首页 → 断言 MobileTabBar 不可见（`md:hidden`） |
| **期望结果** | 移动端显示底部导航，PC 端隐藏 |

#### FI-020：文章详情页左右滑动切换文章

| 字段 | 内容 |
|------|------|
| **测试 ID** | FI-020 |
| **对应功能** | TASK-18 滑动手势 |
| **优先级** | P2 |
| **前置条件** | 移动端视口，文章详情页有上一页/下一篇文章数据 |
| **测试步骤** | 1. 在文章详情页正文区域向左滑动 > 50px<br>2. 断言触发导航到下一篇文章<br>3. 在新文章页向右滑动<br>4. 断言返回上一篇文章 |
| **期望结果** | 滑动手势正确切换上下篇文章 |

---

## 十、Service Worker + 离线

### TASK-15：离线状态检测

#### FI-021：离线时显示提示横条

| 字段 | 内容 |
|------|------|
| **测试 ID** | FI-021 |
| **对应功能** | TASK-15 Service Worker 离线缓存 |
| **优先级** | P1 |
| **前置条件** | 页面已加载，Service Worker 已注册 |
| **测试步骤** | 1. 触发 `offline` 事件（模拟 `window.dispatchEvent(new Event('offline'))`）<br>2. 断言页面顶部出现黄色离线提示 "当前处于离线模式..."<br>3. 触发 `online` 事件<br>4. 断言提示横条消失 |
| **期望结果** | 离线显示提示，恢复在线自动消失 |

#### FI-022：Service Worker 缓存静态资源

| 字段 | 内容 |
|------|------|
| **测试 ID** | FI-022 |
| **对应功能** | TASK-15 Service Worker 离线缓存 |
| **优先级** | P1 |
| **前置条件** | 页面正常加载一次（SW install 完成） |
| **测试步骤** | 1. 打开 DevTools → Application → Cache Storage<br>2. 断言存在 `blog-v1` 缓存<br>3. 断言缓存中包含 `/`、`/articles` 等页面<br>4. 设置 Offline 模式<br>5. 刷新页面<br>6. 断言页面仍能渲染缓存内容 |
| **期望结果** | SW 安装成功，离线可访问已缓存页面 |

---

## 十一、关键 CSS 内联

### TASK-09：内联 CSS 验证

#### FI-023：导航栏样式在内联 CSS 加载后立即生效

| 字段 | 内容 |
|------|------|
| **测试 ID** | FI-023 |
| **对应功能** | TASK-09 关键 CSS 内联 |
| **优先级** | P2 |
| **前置条件** | Chrome DevTools → Network → 设置 Slow 3G |
| **测试步骤** | 1. 清除缓存，设置 Slow 3G<br>2. 刷新首页<br>3. 在 umi.css 加载完成前检查导航栏<br>4. 断言导航栏有深色毛玻璃背景（不是白色）<br>5. 断言导航栏 `position: fixed` 和 `z-index: 50` 生效 |
| **期望结果** | 内联 CSS 确保首屏导航栏在主 CSS 加载前已有正确样式 |

---

## 十二、后台管理集成

### TASK-16：系列管理页面

#### FI-024：系列 CRUD 操作完整流程

| 字段 | 内容 |
|------|------|
| **测试 ID** | FI-024 |
| **对应功能** | TASK-16 文章系列/专栏 |
| **优先级** | P1 |
| **前置条件** | 已登录管理员，Mock `/api/series` 接口 |
| **测试步骤** | 1. 访问 `/admin/series`<br>2. 断言表格显示现有系列列表<br>3. 点击"新建"按钮 → 填写标题 "React 入门" 和描述 → 提交<br>4. 断言 API 发送 `POST /api/series`<br>5. 断言表格刷新，新增一行<br>6. 点击编辑 → 修改标题 → 提交<br>7. 断言 `PUT /api/series/{id}` 被调用<br>8. 点击删除 → 确认<br>9. 断言 `DELETE /api/series/{id}` 被调用，列表移除该行 |
| **期望结果** | 增删改查全流程正常 |
